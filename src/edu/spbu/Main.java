package edu.spbu;

import java.util.*;
import java.io.*;

public class Main {

    public static void main(String[] args) {
        /*Scanner scanner = new Scanner(System.in);
        System.out.println("Введите имя первого текстового файла");
        DMatrix A = new DMatrix(scanner.nextLine());
        System.out.println("Введите имя второго текстового файла");
        DMatrix B = new DMatrix(scanner.nextLine());
        System.out.println(Arrays.deepToString(A.data));
        System.out.println(Arrays.deepToString(B.data));
        try {
            System.out.println(Arrays.deepToString(A.times(B).data));
        } catch (RuntimeException e1) {
            System.out.println("Эти матрицы перемножить нельзя");
        }

        System.out.println("Введите имя первого текстового файла");
        SMatrix C = new SMatrix(scanner.nextLine());
        System.out.println("Введите имя второго текстового файла");
        SMatrix D = new SMatrix(scanner.nextLine());*/
    }
}

